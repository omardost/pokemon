export const Footer = () => {

    return (
        <div className="footer">
            <div style={{display:'flex'}}>

                <button>antes</button>
            </div>
            <div style={{display:'flex'}}>

                <button>despues</button>
            </div>
        </div>
    );
}